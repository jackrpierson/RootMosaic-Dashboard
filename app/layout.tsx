import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RootMosaic - Auto Repair Analytics Dashboard',
  description: 'AI-Powered Detection of Mechanical Misdiagnosis & Technician Inefficiency',
  keywords: 'auto repair, analytics, misdiagnosis, efficiency, dashboard',
  authors: [{ name: 'Jack Pierson' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
} 